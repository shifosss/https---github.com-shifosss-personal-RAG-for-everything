{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.py) command -v ruff >/dev/null 2>&1 && ruff format \"$f\" ;; *.ts|*.tsx|*.js|*.jsx|*.mjs|*.cjs) command -v biome >/dev/null 2>&1 && biome format --write \"$f\" ;; esac; } 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
